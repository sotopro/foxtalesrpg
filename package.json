{
  "name": "learn-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "debug": "NODE_OPTIONS='--inspect' next dev",
    "clean": "rimraf out && rimraf build",
    "build": "next build && next export",
    "deploy": "firebase deploy --only hosting",
    "start": "next start"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "framer-motion": "^5.4.5",
    "next": "^11.0.0",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "rimraf": "^3.0.2"
  }
}
